'use strict';
var calculatePics = function (min, max) {
  return calculatePics = Math.random() * (max - min) + min;
};
var url = calculatePics(1, 25);
var calculateLikes = function (min, max) {
  return Math.random() * (max - min) + min;
};
calculateLikes()
var likes = calculateLikes(15, 200);
var comments = ['Всё отлично!', 'В целом всё неплохо. Но не всё.'];;

var calculateAvatars = function (avatars) {
  var userAvatar = 0;
  for ( var i = 1; i <= avatars.length; i++) {
    userAvatar += avatars[i];
  }
  return userAvatar;
}
var names = calculateAvatars(1, 25);
var calculateNames =
var arrayPictures = [url, likes, comments];
var pictures =

